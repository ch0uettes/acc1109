"""Single source of truth for this app's version, referenced by
VersionMetadata (see app/balance/execution_context.py) so a Decision Log
entry can always be traced back to which code version produced it."""

APP_VERSION = "1.0.0"
